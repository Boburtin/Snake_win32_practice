# Snake (Win32 C++)

## Building

Requires **g++** with C++23 support. Recommended environment: **MSYS2 UCRT64** or **MINGW64**.

```sh
make
```

The binary is output to `bin/snake_win32`.

```sh
make clean  # remove build artifacts
```
